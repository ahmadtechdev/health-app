
import 'package:flutter/material.dart';

const Color wColor = Color(0xFFF4F7FB);
const Color bColor = Colors.black;
const Color sdColor = Colors.black12;
const Color secondary = Color(0xFF2E3192);
// const Color g1Color = Color(0xFF2E3192 );
const Color primary = Color(0xFF0C84FF);
const Color p1Color = Color(0xFF00E0CE);
const Color y1Color = Color(0xFF8396A8);

class TColors {
  // Core Colors (slightly deeper lavender tones)
  static const Color primary = Color(0xFFB9A8D6);      // Slightly darker lavender
  static const Color secondary = Color(0xFFE6DEF4);    // Deeper light lavender
  static const Color accent = Color(0xFF9E8CC8);       // More pronounced accent lavender

  // Backgrounds
  static const Color background = Color(0xFFF7F5FB);   // Off-white with lavender tint
  static const Color background2 = Color(0xFFF0EBF7);  // Slightly deeper than before
  static const Color background3 = Color(0xFFE0D8EF);  // More contrast for cards/panels
  static const Color background4 = Color(0xFFBBA0D0);  // More visible header/footer tone

  // Text Colors (boosted contrast)
  static const Color textPrimary = Color(0xFF1F1F1F);  // Darker for better readability
  static const Color textSecondary = Color(0xFF555555); // Richer grey for secondary text
  static const Color placeholder = Color(0xFF888888);   // More visible input hint

  // Greyscale
  static const Color greyLight = Color(0xFFCCCCCC);    // Slightly darker light grey
  static const Color grey = Color(0xFF7A7A7A);          // Standard grey
  static const Color greyDark = Color(0xFF4A4A4A);      // Deeper grey

  // Feedback Colors (darker pastels)
  static const Color success = Color(0xFF7FBF98);       // Richer mint green
  static const Color warning = Color(0xFFFAD663);       // More readable yellow
  static const Color error = Color(0xFFFF8C8C);         // More noticeable blush red

  // Neutrals
  static const Color white = Color(0xFFFFFFFF);
  static const Color black = Color(0xFF000000);
}

