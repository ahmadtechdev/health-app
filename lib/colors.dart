
import 'package:flutter/material.dart';

const Color wColor = Color(0xFFF4F7FB);
const Color bColor = Colors.black;
const Color sdColor = Colors.black12;
const Color secondary = Color(0xFF2E3192);
// const Color g1Color = Color(0xFF2E3192 );
const Color primary = Color(0xFF0C84FF);
const Color p1Color = Color(0xFF00E0CE);
const Color y1Color = Color(0xFF8396A8);

class TColors {
  // Core Colors
  static const Color primary = Color(0xFFD6CDEA);      // Soft Lavender (main brand color)
  static const Color secondary = Color(0xFFF4F1FA);    // Very light lavender/white blend
  static const Color accent = Color(0xFFBBAEDB);       // Slightly deeper lavender for accents

  // Backgrounds
  static const Color background = Color(0xFFFFFFFF);   // Main background - pure white
  static const Color background2 = Color(0xFFF9F8FC);  // Subtle lavender-tinted white
  static const Color background3 = Color(0xFFEFEAF6);  // For cards, panels, etc.
  static const Color background4 = Color(0xFFCFC3E0);  // Soft contrast background (e.g., footer/header)

  // Text Colors
  static const Color textPrimary = Color(0xFF2D2D2D);  // Almost black, clean for readability
  static const Color textSecondary = Color(0xFF6E6E6E); // Mid-grey for secondary text
  static const Color placeholder = Color(0xFFA0A0A0);   // Input hints, etc.

  // Greyscale
  static const Color greyLight = Color(0xFFE0E0E0);
  static const Color grey = Color(0xFF9E9E9E);
  static const Color greyDark = Color(0xFF616161);

  // Feedback Colors (very soft and pastel-style)
  static const Color success = Color(0xFFA8D5BA);      // Soft mint green
  static const Color warning = Color(0xFFFFE29B);      // Light pastel yellow
  static const Color error = Color(0xFFFFB6B6);        // Soft blush red

  // Neutrals
  static const Color white = Color(0xFFFFFFFF);
  static const Color black = Color(0xFF000000);
}
